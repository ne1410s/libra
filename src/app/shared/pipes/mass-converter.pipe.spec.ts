import { MassConverterPipe } from './mass-converter.pipe';

describe('MassConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new MassConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
